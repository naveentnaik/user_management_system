<% include("../layouts/header.ejs") %> 

<h1>Dashboard</h1>

<a href="/admin/new-user">add new user</a>
<br><br>



<table width="50%">
   <tr>
    <th>Name</th>
    <th>Email</th>
    <th>mobile</th>
    <th>Image</th>
    <th>Verified</th>
   </tr>

   <% 
   if(users.length>0){
            for(let i=0;i<users.length;i++){
            %>
            <tr>
                <td><%=users[i].name %></td>
                <td><%=users[i].email %></td>
                <td><%=users[i].mobile %></td>
                <td> <img src="/userImages/<%= users[i].image%>" alt="<%= users[i].image%>" width="100px" height="100px"<%=users[i].image %></td>
                <td><%if(users[i].is_varified===0){%>no <% }else{%>yes<% } %></td>
           </tr>
           <%
            }

   }
   else{
    %>

    <tr>
        <td colspan="5">User not found</td>
    </tr>

    <%

   }
   
   
   %> 
 
</table>

<a href="/admin/home">Go to home</a>

<% include("../layouts/footer.ejs") %> 